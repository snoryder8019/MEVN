<!DOCTYPE html>

<head>
  <%- include('config/head.ejs')%>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body style="background-color: rgba(0,0,0,1); color: rgb(255, 255, 255);">

  <div class="wixGrid">
    <%for(let i =0;i<data[0].length;i++){%>  
<%if(data[0][i].status=="show" && data[0][i].serviceCategory=="dubSquaredMedia"){%>

      <%let svcName = data[0][i].serviceName%>
      <%let svcNameSlice = svcName.slice(0,21)%>

<div class="dubContainer">

  
  <div class="wixTitle">
  <h1><%=data[0][i].serviceName%></h1> 

</div>
<h2 class="divider">$<%=data[0][i].cost%>.00 <span class="accentDubStyle">|</span> <%=data[0][i].terms%></h2>

<!--PARSE SERVICE DETAILS WITH &&-->
<div class="wixBody">
  <%let svcDetail = data[0][i].serviceDetails%>
  <%let svcStr = svcDetail.toString()%>
  <%let svcSlice = svcDetail.split("&&")%>
  <%if(svcSlice.length>=1 || svcSlice!==null){%>
  <ul>
  <% for (let i = 1; i < svcSlice.length; i++) { %>
   <li>   <%= svcSlice[i] %></li>
 
  <% } %>
</ul>
<%}%>
</div>

</div>
<%}%>
<%}%>
</div>

</body>
</html>